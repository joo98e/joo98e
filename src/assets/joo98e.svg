<svg width="800" height="125" xmlns="http://www.w3.org/2000/svg">
    <!-- Define text with stroke and fill, center aligned -->
    <text x="50%" y="50%" font-family="'Dancing Script', cursive" font-size="72" fill="none" stroke="white" stroke-width="3" text-anchor="middle" alignment-baseline="middle">
        🤙 Joo98e's Contact
    </text>

    <!-- Animation -->
    <style>
        text {
        stroke-dasharray: 1000; /* Increase length of stroke path */
        stroke-dashoffset: 1000; /* Increase the starting offset */
        animation: draw 6s ease forwards; /* Slow down animation for a longer effect */
        }

        @keyframes draw {
        100% {
        stroke-dashoffset: 0;
        }
        }
    </style>
</svg>